<% const { modal, actions } = locals.createCycleForm; %>

<%- include("../../shared/components/button/button", { button: {
	label: actions.cancel.label,
	variant: "secondary",
	type: "button",
	attributes: { "data-modal-close": modal.id }
} }) %>

<%- include("../../shared/components/button/button", { button: {
	label: actions.submit.label,
	variant: "primary",
	type: "submit",
	disabled: actions.submit.disabled
} }) %>
