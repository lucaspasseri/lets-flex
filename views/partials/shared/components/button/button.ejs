<%
	const {
		id,
		type = "button",
		label,
		attributes = {},
		renderIcon,
		iconAfterText = false,
		iconOnly = false,
		variant = null,
		disabled = false,
		className = null
	} = locals.button;

	const additionalClass = variant || className;
%>

<button
	<% if (id) { %>id="<%= id %>"<% } %>
	type="<%= type %>"
	class="shared-button<%= iconAfterText ? ` reverse` : '' %><%= additionalClass ? ` ${additionalClass}` : ''%>"
	<% if (iconOnly) { %>aria-label="<%= label %>"<% } %>
	<% if (disabled) { %><%= "disabled"  %><% } %>

	<% Object.entries(attributes).forEach(([name, value]) => { %>
		<% if (value === true || value === null) { %>
			<%= name %>
		<% } else if (value !== false && value !== undefined) { %>
			<%= name %>="<%= value %>"
		<% } %>
	<% }) %>
	
>
	<% if (renderIcon) { %>
		<%- renderIcon() %>
	<% } %>

	<% if (!iconOnly) { %>
		<span class="label"><%= label %></span>
	<% } %>
</button>