<% const shape = locals.status === "performed" ? "check-circle" : locals.status === "skipped" ? "minus-circle" : "circle"; %>
<%- include("icon", { icon: { attributes: locals.icon?.attributes, label: locals.icon?.label, content: include(`shapes/${shape}`) } }) %>
