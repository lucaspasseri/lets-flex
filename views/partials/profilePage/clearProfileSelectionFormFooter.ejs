<% const { modal, form } = locals.clearSelectionAction; %>

<%- include("../shared/components/button/button", { button: {
	label: form.actions.cancel.label,
	variant: "secondary",
	type: "button",
	attributes: { "data-modal-close": modal.id }
} }) %>

<%- include("../shared/components/button/button", { button: {
	label: form.actions.submit.label,
	variant: "primary",
	type: "submit"
} }) %>
