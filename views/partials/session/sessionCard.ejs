<article class="session-card session-card-<%= card.type %>">
	<%- include("./sessionHeader", { header: card.header }) %>
	<p>SESSION STEPS</p>
	<%- include("./stepList", { steps: card.steps }) %>
</article>
