<% const username = locals.appState?.currentUser?.name ?? "Guest" %>

<header class="page-header">
	<div class="logoContainer"><%- include('logo'); %></div>
	<div>
		<div class="avatar"></div>
		<div class="avatar-name">
			<p><%= username %></p>
		</div>
	</div>
</header>
