<%
	const {
		message = "No session templates yet.",
		icon = "list-ordered"
	} = locals.emptyState;
%>

<div class="session-summaries__empty">
	<div class="session-summaries__empty-icon-container" aria-hidden="true">
		<i data-lucide="<%= icon %>"></i>
	</div>
	<p><%= message %></p>
</div>
