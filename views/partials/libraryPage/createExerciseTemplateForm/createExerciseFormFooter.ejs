<%
	const { modal, actions } = locals.exerciseForm;
%>


<%- include("../../shared/components/button/button",  {button: {
		label: actions.cancel.label,
		type: "button",
		variant: "secondary",
		attributes: {
			"data-modal-close" : modal.id
		}
	}}) 
%> 
<%-
include("../../shared/components/button/button", {button: {
		label: actions.submit.label,
		variant: "primary",
		type: "submit"
	}}) 
%>
