<% const text = data?.text || "Button"; const type = data?.type || "button";
const id = data?.id || ""; const disabled = data?.disabled ? "disabled" : ""; %>

<button id="<%= id %>" class="simple-button" type="<%= type %>" <%= disabled %>>
	<%= text %>
</button>
