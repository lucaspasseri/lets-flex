/**
 * @typedef {import("../users/users.types.js").User} User
 */

/**
 * Application data required to present the profile page.
 * It contains no labels, links, modal identifiers, or component configuration.
 *
 * @typedef {object} ProfilePageData
 * @property {User | null} currentUser
 * @property {User[]} users
 */

export {};
